// Import and re-export the constants
const { VIEW } = require("./viewName");
const { APP_MESSAGE } = require("./appConstants");
const { COOKIE_VALUE } = require("./cookieValue");
const { APP_SECRET } = require("./appSecret");
const { ROLES } = require("./role");


module.exports = {
    VIEW,
    APP_MESSAGE,
    COOKIE_VALUE,
    APP_SECRET,
    ROLES,
};
